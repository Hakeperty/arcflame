[workspace]
resolver = "2"
members = [
    "node-agent",
    "tools/gguf-splitter",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"

[profile.release]
lto = true
codegen-units = 1
strip = "symbols"
